[flake8]
max-line-length = 100
# E203 and W503 conflict with black's formatting; E731 (lambda assignment) is
# used intentionally in a few tests/demos and silenced inline with `# noqa`.
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    build,
    dist,
    .venv,
    venv
