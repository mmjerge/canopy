# Pre-commit hooks for canopy.
# Install once with:  pre-commit install
# Run on all files:   pre-commit run --all-files
ci:
  # pre-commit.ci runs hooks in isolated envs without the project installed, so the
  # local (system) mypy hook can't resolve imports there; run mypy in GitHub Actions.
  skip: [mypy]
  autoupdate_schedule: monthly

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-added-large-files
        args: ["--maxkb=1024"]

  - repo: https://github.com/psf/black
    rev: 26.5.1
    hooks:
      - id: black

  - repo: https://github.com/pycqa/flake8
    rev: 7.3.0
    hooks:
      - id: flake8

  - repo: local
    hooks:
      - id: mypy
        name: mypy
        entry: mypy
        language: system
        types: [python]
        pass_filenames: false
        args: ["src/canopy"]
